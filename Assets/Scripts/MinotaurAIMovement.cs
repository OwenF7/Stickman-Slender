using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class MinotaurAIMovement : MonoBehaviour
{
    //public NavMeshAgent minotaur;
    //public Transform minotaurDestination;
    //public float speed = 1f;
    //public float acceleration = 1f;

    // Start is called before the first frame update
    void Start()
    {
        //minotaur.speed = speed;
    }

    // Update is called once per frame
    void Update()
    {
        //minotaur.SetDestination(minotaurDestination.position);
        //minotaur.acceleration = acceleration;
    }
}
